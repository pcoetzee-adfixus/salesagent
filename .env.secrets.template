# AdCP Sales Agent - Secrets Configuration Template
# =================================================
# Copy this file to .env.secrets and fill in your actual values:
#   cp .env.secrets.template .env.secrets
#
# To use with docker-compose, source the file before running:
#   source .env.secrets && docker-compose up -d
#
# Or export variables individually in your shell.

# ============================================
# REQUIRED - App will not start without these
# ============================================

# Gemini API Key (get from https://aistudio.google.com/apikey)
GEMINI_API_KEY=your-gemini-api-key-here

# Super Admin email(s) - comma separated, these users get full system access
SUPER_ADMIN_EMAILS=your-email@example.com

# ============================================
# REQUIRED FOR ADMIN UI LOGIN (Google OAuth)
# ============================================

# Google OAuth credentials (from Google Cloud Console)
# Create at: https://console.cloud.google.com/apis/credentials
# Authorized redirect URIs must include:
#   - Docker standalone: http://localhost:8001/auth/google/callback
#   - Production: https://your-domain.com/admin/auth/google/callback
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret

# ============================================
# OPTIONAL
# ============================================

# Allowed admin domains (users from these domains can access any tenant)
SUPER_ADMIN_DOMAINS=example.com

# GAM OAuth (only needed for Google Ad Manager integration)
GAM_OAUTH_CLIENT_ID=your-gam-client-id.apps.googleusercontent.com
GAM_OAUTH_CLIENT_SECRET=your-gam-client-secret
